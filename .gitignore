# ---- Existing ----
*.zip
resiliency-static.zip

# ---- Node / build tools ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
# Keep package.json tracked (it documents build deps) — uncomment next line to ignore:
# package.json

# ---- Local-only files ----
.env
.env.local
.env.*.local

# ---- Editor / OS ----
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
.cache/

# ---- Build artifacts (NOT css/tailwind-built.css — that one is committed) ----
# Add any other transient build outputs here
.tmp/
.temp/
